name = "recall-api"
main = "worker.mjs"
compatibility_date = "2024-06-01"
account_id = "f3ef8ef6ea55d1d3c0e408d00d4a6c57"

# Cloudflare Workers AI — runs Llama on CF edge (no Groq key required)
[ai]
binding = "AI"

[vars]
# Optional secrets for fallbacks (set via `wrangler secret put`):
# GROQ_API_KEY, ELEVENLABS_API_KEY, GOOGLE_VISION_KEY
